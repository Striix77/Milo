using UnityEngine;

public class PeaBoomFollow : MonoBehaviour
{
    // public Transform AttackPosition;

    // Update is called once per frame
    void Update()
    {
        // transform.position = AttackPosition.position;
    }
}
